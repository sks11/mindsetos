# NextAuth Configuration
NEXTAUTH_SECRET=your_nextauth_secret_here_generate_with_openssl_rand_base64_32
NEXTAUTH_URL=http://localhost:3000

# Google OAuth Configuration
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Instructions:
# 1. Copy this file to .env.local (for Next.js) or .env (for Python scripts)
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit the actual .env files to version control
# 4. For NEXTAUTH_SECRET, generate a secure random string: openssl rand -base64 32
# 5. Get Google OAuth credentials from Google Cloud Console
# 6. Get OpenAI API key from OpenAI platform
